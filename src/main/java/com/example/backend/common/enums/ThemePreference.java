package com.example.backend.common.enums;

/**
 * Preferencia de tema (claro/oscuro) del usuario, persistida en su perfil
 * para que se mantenga consistente entre dispositivos.
 */
public enum ThemePreference {
    LIGHT,
    DARK
}
